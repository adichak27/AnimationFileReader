package model;

/**
 * this enum represents the type of actions that can be done on a shape in
 * an Model.EasyAnimator model.
 */
public enum TransformationType {
  Move, ChangeSize, ChangeColor, DoNothing
}
